###这本来是重新捡起来的东西，但没想到出现的错误比第一次接触的时候还要多

#####1、编译环境

        * 还是最好按视频走吧，我下的最新版的ndk（大概是20吧，没有编译成功）

        * ffmpeg的版本也先按视频走，后期等功能做完再升级，api变化不大

        * 注意编译脚本中ndk路径问题

        * 注意编译脚本中把linux-x86_64改成darwin-x86_64（我用的mac编译的）

#####2、abiFilters的问题

        * 最好在app和module中都进行配置
